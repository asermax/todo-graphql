# Todo List app using graphql + react

The repo contains files to run from docker.

To setup the app:

```
docker-compose -f docker-compose.setup.yml up
```

To run the app:

```
docker-compose up
```
